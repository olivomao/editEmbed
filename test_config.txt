#---------- this is a test config file

#---------- location

root_dir                		data_sim_data_type_bin/
data_label              		case_bin_sub001_ins0_del0_batch_test_data
model_label             		tune_hdsz_lrate

#---------- general

seq_type                		0

#---------- data (clusters)

n_clusters						100
cluster_len						150
weight_distr					0

#---------- data (samples)

sid_pre							cc 							#prefix for clusers
sample_prefix					sp 							#prefix for samples
n_tot_samples					-1 							#if >0 then each cluster has n_tot_samples*weight seqs to be sampled
n_copy							10							
clear_split						1 							#only used for multi-threads

rate_ins						0.00                        #indel channel config
rate_del						0.00
rate_sub						0.01

#---------- data (dist) train

dist_tp_list_data				0							#0:edit 3:nn_dist
add_hd							1
clear_interm					1
max_num_dist_1thread			-1

#---------- training

max_num_to_sample       		-1

load_model              		0

batch_size              		100
num_epochs              		500

maxlen                  		500
blocklen                		10

embedding_size          		200
num_layers              		3
hidden_sz               		300,250,200,150,100

learning_rate           		0.005,0.0002,0.0001

dropout                 		1.0

#---------- evaluation

dist_tp_list_eval				0,2,3						#0:edit 2:cgk 3:nn_dist
add_hd_eval						1
clear_interm_eval				1
max_num_dist_1thread_eval		-1

disc_cols						na 							#for hist
normalized_hist					1							

n_thresholds					100							#for ROC
normalized_roc					1


